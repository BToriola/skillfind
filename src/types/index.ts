export type Freelancer = {
  id: string;
  user_id: string;
  name: string;
  skill: string;
  category: string;
  state: string;
  bio: string;
  rate: string;
  whatsapp: string;
  portfolio: string;
  avatar_url: string | null;
  is_approved: boolean;
  created_at: string;
};